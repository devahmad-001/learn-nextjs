import React from 'react'

export default function layout({ children }) {
    return (
        <div>
        THis is the navbar in the layout
        {children}
        THis is the footer in layout
        </div>
    )
}
